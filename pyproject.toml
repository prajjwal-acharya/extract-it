[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "doc-intel-platform"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0",
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
    "psycopg[binary]>=3.1.0",
    "pgvector>=0.3.0",
    "langgraph>=0.1.0",
    "langsmith>=0.1.0",
    "google-cloud-aiplatform>=1.55.0",
    "google-cloud-pubsub>=2.21.0",
    "google-cloud-storage>=2.17.0",
    "minio>=7.2.0",
    "watchdog>=4.0.0",
    "streamlit>=1.36.0",
    "python-multipart>=0.0.9",
    "pyyaml>=6.0.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=5.0.0",
    "httpx>=0.27.0",
    "vcrpy>=6.0.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = ["live: calls real LLM API (deselect with -m 'not live')"]
